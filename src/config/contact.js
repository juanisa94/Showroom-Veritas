/** E.164 numbers; digits-only used for https://wa.me/<digits>. Política: mensajes únicamente. */
export const CONTACT = Object.freeze({
  channels: Object.freeze([
    Object.freeze({
      e164: '+34695056887',
      subtitle: 'Solo para Mensajes',
    }),
    Object.freeze({
      e164: '+34624266445',
      subtitle: 'Solo para Mensajes',
    }),
  ]),
})
